using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HighlightText : MonoBehaviour
{

    //the best stratedgy im seeing right now is to mirror the text with a highlight underneath the current text.
    //since you can toggle speaker colors on and off, maybe i should just have th eoptions here be plain black or the like oppisute color of the text
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
